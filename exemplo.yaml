server:
  http_addr: ":8080"
  public_url: "http://my-server:8080"

jwt:
  secret: "change-me"

forge:
  type: github
  app_id: 12345
  private_key: ./private-key.pem
  webhook_secret: "secret"

runner:
  type: nomad
  addr: "http://localhost:4646"
  job_name: "mise-ci-run"
  default_image: "ghcr.io/mise-ci/worker:latest"

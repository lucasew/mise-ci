[tasks."codegen:protobuf"]
description = "Gera código do protobuf"
run = "protoc --go_out=. --go-grpc_out=. proto/ci.proto"

[tasks.codegen]
description = "Gera todo código necessário"
depends = ["codegen:protobuf"]

[tasks.build]
description = "Compila binário"
run = "go build -o bin/mise-ci ./cmd/mise-ci"

[tasks.ci]
description = "Roda checks de CI"
depends = ["ci:lint", "ci:test"]

[tasks."ci:lint"]
run = "golangci-lint run"

[tasks."ci:test"]
run = "go test ./..."
